import { describe, it, expect } from 'vitest';

describe("matematica básica", () => {
    it("Soma 2 + 2", () => {
        expect(2+2).toBe(4);
    })

    it("Multiplicação 3 . 3", () => {
        expect(3*3).toBe(9);
    })
})